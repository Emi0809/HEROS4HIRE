
  <div class="back-button d-flex justify-content-start">
    <%= link_to "Back to Superheros", superheros_path, class:"back-to-hero" %>
  </div>
<div class="container d-flex", style='justify-content: center'>
  <div class="card-trip col-6">
    <h2 class="card-title"><%= @superhero.name %></h2>
    <img src="<%= @superhero.photo_url %>" class="img-show" >
    <%= link_to superhero_path(@superhero), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this workout log?" } do %>
                <i class="fa-solid fa-trash" style = "width: 20px; height: 20px; position:relative; left:110px; bottom:90px;"></i>
                <% end %>
                </div>
  <div class= "col">

    <div class="card-details">
      <p class="card-text"><strong>Description: </strong><%= @superhero.description %></p>
      <p class="card-text"><strong>Superpower: </strong><%= @superhero.superpower %></p>

      <p class="card-trip-pricing"><strong>Price: </strong>£<%= @superhero.price %></p>

    </div>

    <div class= "card-booking col">
      <%= simple_form_for [@superhero, @booking] do |f| %>
      <%= f.input :start_date, html5: true %>
      <%= f.input :end_date, html5: true %>
      <%= f.submit "Book Superhero", class:"btn btn-primary" %>
      <% end %>
    </div>

  </div>
</div>



<div data-controller="map"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
  data-map-markers-value="<%= @markers.to_json %>"
  id='map'
  style='width: 100%;
  height: 300px;'>
</div>
